language = "nodejs"
run = ["npm", "run", "start"]

entrypoint = "README.md"

[env]
FOO="foo"

[packager]

  [packager.features]
  # Enables the package search sidebar
  packageSearch = false
  # Enabled package guessing
  guessImports = false